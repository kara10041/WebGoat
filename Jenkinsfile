pipeline {
    agent any

    stages {
        stage('Echo Test') {
            steps {
                echo "✅ Jenkins 파이프라인이 정상적으로 실행되고 있습니다."
                sh 'echo "✅ 쉘 명령도 실행됩니다."'
            }
        }
    }
}
